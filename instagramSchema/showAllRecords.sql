# select all records comments table
SELECT * FROM comments LIMIT 3;

# select all records likes (join between users and photos)
SELECT * FROM likes LIMIT 3 ;

# select all records photoTags (join between photos and hashtags)
SELECT * FROM photoTags LIMIT 3 ;

# select all records hashtags ()
SELECT * FROM hashtags LIMIT 3 ;

# select all records photos table
SELECT * FROM photos LIMIT 3 ;

# select all records followers (join users and users)
SELECT * FROM follows LIMIT 3 ;

# select all records users table
SELECT * FROM users LIMIT 3 ;